---
version: "3.8"

services:
  backend:
    labels:
      - "traefik.http.routers.${PROJECT_NAME}.rule=Host(`${DOMAIN_NAME_WITHOUT_TLD}.localhost`)"

networks:
  traefik-global-proxy:
    external: true
